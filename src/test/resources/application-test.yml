spring:
  datasource:
    url: jdbc:h2:mem:garage;Mode=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

flyway:
  enabled: true

garage:
  client:
    base:
      url: http://localhost:9999